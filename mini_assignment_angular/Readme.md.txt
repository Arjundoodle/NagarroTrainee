To run the frontend first install the node_modules through npm-i in the visual studios in the Mini_Assignment


then run the angular_backend in visual studio code ,build the project and if then also databases are not being created remove the migrations and Add-Migration and use update-database

To connect to mssql check that the connection string has CarRental3 as the database if not create it and apply the connection string accordingly